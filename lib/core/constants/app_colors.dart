import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF0A1F44); // deep navy
  static const Color secondary = Color(0xFF00D4C8); // electric teal
  static const Color background = Color(0xFFF4F6F9); // cool light grey
  static const Color surface = Color(0xFFFFFFFF); // pure white for cards
  static const Color error = Color(0xFFFF4D4F); // bright red
  static const Color success = Color(0xFF00C48C); // modern green
  static const Color textPrimary = Color(0xFF0A1F44); // same as primary for consistency
  static const Color textSecondary = Color(0xFF6B7A99); // muted blue grey
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF0A1F44);
  static const Color shimmer = Color(0xFFE8ECF4); // cool grey for loading shimmer
  static const Color star = Color(0xFFFFBB00); // gold for ratings
  static const Color divider = Color(0xFFE8ECF4); // soft divider
  static const Color cardShadow = Color(0x1A0A1F44); // navy shadow with low opacity
}
